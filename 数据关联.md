# 数据关联与自动化逻辑设计 (Data Relationships & Automation Design)

本文档记录了系统中关键的数据关联规则与自动化流转逻辑，旨在确保业务流程的连贯性与数据的实时一致性。

## 1. 客户生命周期流转 (Customer Lifecycle Automation)

### 1.1 线索 -> 潜在客户 (Lead to Potential)
**业务场景**：
当销售人员首次联系线索客户（Lead）并收到对方的有效回复时，该客户即被视为进入“潜在客户”（Potential）阶段，具备进一步开发的价值。

**设计逻辑**：
- **触发入口**：客户详情页 -> 动态成长/维护日志 (Engagement Log) -> 检查阶段 (Check)。
- **操作交互**：用户勾选“客户已回复 (Customer Replied)”选项。
- **数据变更**：
  1.  **状态变更**：客户档案的 `tier` 字段由 `lead` (线索) 自动更新为 `probation` (潜在客户)。
  2.  **健康度重置**：清除线索阶段特有的 `healthStatus` (如“未触达”、“待响应”)，转而启用基于评分模型的健康度系统。
  3.  **日志记录**：自动在当次沟通日志的“主要成果”中追加“(客户已回复，自动晋级为潜在客户)”字样，形成审计轨迹。

### 1.2 潜在 -> 成交客户 (Potential to Closed)
*(规划中)*
- **触发条件**：系统中录入首个有效订单（Booking）或签署正式合同。
- **数据变更**：`tier` 变更为 `core` (成交客户)，开启账期与额度管理模块。

---

## 2. 动态日志与档案同步 (Log-to-Profile Sync)

**业务场景**：
销售在拜访客户后，往往获取了最新的企业信息（如搬迁新址、新决策人上任）。为了避免重复录入，系统支持在写日志时直接更新档案。

**设计逻辑**：
- **Smart Diff 机制**：
  - 日志弹窗中包含“全维信息更新”模块。
  - 系统实时比对“原档案数据”与“日志中修改的数据”。
  - 仅将差异项（Diff）作为结构化数据保存至日志的 `dimensions` 字段，同时更新主档案。
- **数据一致性**：
  - 档案更新（`setSupplier`）与日志保存（`setEngagementLogs`）为原子操作，确保视图与历史记录同步。

---

## 3. 决策链与资源关联 (Decision Chain & Resources)

**业务场景**：
维护客户关系本质上是维护“人”的关系。系统将关键联系人（Key Person）与外部资源（Resources）进行强关联。

**数据模型**：
- **Person (关键人)**：
  - 属性：亲密度 (Affinity)、角色 (Role: Decision Maker/Influencer)、层级 (Layer)。
- **Resource (衍生资源)**：
  - 归属：每个资源必须挂载在某个具体的 Person 下（例如：Jason 是“物流协会”的理事）。
  - 价值：资源可被独立搜索和统计，用于分析客户的潜在影响力（Derivative Value）。
